<Page x:Class="HRManagementSystem.Views.Admin.WelcomeView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Background="#F8FAFC"
      Title="WelcomeView">

    <Grid Margin="24">

        <Border Background="White"
                CornerRadius="16"
                Padding="32"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch">

            <Border.Effect>
                <DropShadowEffect BlurRadius="16" Opacity="0.12"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Welcome to HR Management System"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="#0F172A"/>

                <TextBlock Grid.Row="1"
                           Text="Choose a module from the sidebar to get started."
                           Margin="0,12,0,0"
                           Foreground="#64748B"
                           FontSize="14"/>

                <Grid Grid.Row="2" Margin="0,24,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="#F8FAFC" CornerRadius="12" Padding="16" Margin="0,0,12,0">
                        <StackPanel>
                            <TextBlock Text="Employees" FontWeight="SemiBold"/>
                            <TextBlock Text="Manage employee profiles" Foreground="#64748B" Margin="0,6,0,0"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Background="#F8FAFC" CornerRadius="12" Padding="16" Margin="0,0,12,0">
                        <StackPanel>
                            <TextBlock Text="Departments" FontWeight="SemiBold"/>
                            <TextBlock Text="Organize teams and roles" Foreground="#64748B" Margin="0,6,0,0"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="2" Background="#F8FAFC" CornerRadius="12" Padding="16">
                        <StackPanel>
                            <TextBlock Text="Attendance" FontWeight="SemiBold"/>
                            <TextBlock Text="Track daily attendance" Foreground="#64748B" Margin="0,6,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>

        </Border>

    </Grid>

</Page>
