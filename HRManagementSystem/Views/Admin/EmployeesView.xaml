<Page x:Class="HRManagementSystem.Views.Admin.EmployeesView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:HRManagementSystem.Views.Admin"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="EmployeesView">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2.3*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" 
                Margin="0,0,10,0" 
                Padding="10" 
                Background="White" 
                BorderBrush="#D0D7E2" 
                BorderThickness="1" 
                CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBox Width="220"
                             Margin="0,0,8,0"
                             VerticalContentAlignment="Center"/>
                    <Button Content="Search" Width="95" Margin="0,0,8,0"/>
                    <Button Content="Refresh" Width="95" Margin="0,0,8,0"/>
                    <Button Content="New" Width="95" Margin="0,0,8,0"/>
                    <Button Content="Delete" Width="95"/>
                </StackPanel>

                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding Employees}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Width="70" />
                        <DataGridTextColumn Header="Full Name"  Width="*" />
                        <DataGridTextColumn Header="DOB" Width="120" />
                        <DataGridTextColumn Header="Gender"  Width="95" />
                        <DataGridTextColumn Header="Phone" Width="120" />
                        <DataGridTextColumn Header="Department" Width="140" />
                        <DataGridTextColumn Header="Position" Width="160" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border Grid.Column="1" Padding="14" Background="White" BorderBrush="#D0D7E2" BorderThickness="1" CornerRadius="8">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Employee Detail" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,14" />

                    <TextBlock Text="Full Name" />
                    <TextBox x:Name="FullNameBox"
                             Margin="0,6,0,2"
                             Text="{Binding FullName}" />
                    <Border Height="6" Background="Transparent" />

                    <TextBlock Text="Date of Birth" />
                    <DatePicker x:Name="DateOfBirthPicker"
                                Margin="0,6,0,2"/>
                    <Border Height="6" Background="Transparent" />

                    <TextBlock Text="Gender" />
                    <ComboBox Margin="0,6,0,10" />

                    <TextBlock Text="Phone" />
                    <TextBox x:Name="PhoneBox"
                             Margin="0,6,0,2"
                             Text="{Binding Phone}" />
                    <Border Height="6" Background="Transparent" />

                    <TextBlock Text="Department" />
                    <ComboBox x:Name="DepartmentBox"
                              Margin="0,6,0,2"/>
                    <Border Height="6" Background="Transparent" />

                    <TextBlock Text="Position" />
                    <ComboBox x:Name="PositionBox"
                              DisplayMemberPath="Name"
                              Margin="0,6,0,2"
                              IsSynchronizedWithCurrentItem="False" />
                    <Border Height="12" Background="Transparent" />

                    <Button Content="Save" Height="34"/>

                    <TextBlock Text="{Binding StatusMessage}" Margin="0,12,0,0" Foreground="#334155" TextWrapping="Wrap" />
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>
