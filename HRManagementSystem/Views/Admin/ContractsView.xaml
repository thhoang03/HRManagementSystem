<Page x:Name="frmContracts" x:Class="HRManagementSystem.Views.Admin.ContractsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:HRManagementSystem.Views.Admin"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ContractsView"
      Background="#F8FAFC" Loaded="frmContracts_Loaded">

    <Grid Margin="24">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Contracts Management"
                   Grid.Row="0"
                   FontSize="24"
                   FontWeight="Bold"
                   Foreground="#0F172A"
                   VerticalAlignment="Center"/>

        <StackPanel Grid.Row="1" Margin="0,16,0,16" Orientation="Horizontal">

            <Button x:Name="btnDelete"
                    Content="Deactive"
                    Width="100"
                    Height="36"
                    Margin="0,0,8,0"
                    Background="#EF4444"
                    Foreground="White"
                    Click="btnDelete_Click"/>

            <Button x:Name="btnActive"
                    Content="Active"
                    Width="100"
                    Height="36"
                    Margin="0,0,16,0"
                    Background="#22C55E"
                    Foreground="White"
                    Click="btnActive_Click"/>

            <TextBox x:Name="txtSearch"
                     Width="220"
                     Height="36"
                     Padding="8"
                     Margin="0,0,8,0"
                     VerticalContentAlignment="Center"
                     TextChanged="txtSearch_TextChanged"/>

            <Button Content="Search"
                    Width="90"
                    Height="36"
                    Background="#F9D678"
                    FontWeight="SemiBold"/>

        </StackPanel>

        <Border Grid.Row="2"
                Background="White"
                CornerRadius="12"
                Padding="16">

            <Border.Effect>
                <DropShadowEffect BlurRadius="12" Opacity="0.15"/>
            </Border.Effect>

            <DataGrid x:Name="dgContracts"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      RowHeight="34"
                      HeadersVisibility="Column"
                      GridLinesVisibility="None">

                <DataGrid.Columns>

                    <DataGridTextColumn Header="ID"
                                        Binding="{Binding ContractId}"
                                        Width="90"/>

                    <DataGridTextColumn Header="Employee"
                                        Binding="{Binding Employee.FullName}"
                                        Width="2*"/>

                    <DataGridTextColumn Header="Type"
                                        Binding="{Binding ContractType}"
                                        Width="1*"/>

                    <DataGridTextColumn Header="Start Date"
                                        Binding="{Binding StartDate}"
                                        Width="1*"/>

                    <DataGridTextColumn Header="End Date"
                                        Binding="{Binding EndDate}"
                                        Width="1*"/>

                    <DataGridTextColumn Header="Salary"
                                        Binding="{Binding ContractSalary}"
                                        Width="1*"/>

                    <DataGridTextColumn Header="Status"
                                        Binding="{Binding Status}"
                                        Width="1*"/>

                </DataGrid.Columns>

            </DataGrid>

        </Border>

    </Grid>
</Page>
