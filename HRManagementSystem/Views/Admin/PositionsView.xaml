<Page x:Name="frmPositions" x:Class="HRManagementSystem.Views.Admin.PositionsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Background="#F8FAFC" Loaded="frmPositions_Loaded">

    <Grid Margin="24">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <!-- HEADER -->

        <TextBlock Text="Position Management"
                   Grid.Row="0"
                   FontSize="24"
                   FontWeight="Bold"
                   Foreground="#0F172A"
                   VerticalAlignment="Center"/>

        <StackPanel Grid.Row="1" Margin="0,16,0,16" Orientation="Horizontal">

            <Button x:Name="btnNew"
                        Content="+ New"
                        Width="100"
                        Height="36"
                        Margin="0,0,8,0"
                        Background="#F9D678"
                        FontWeight="SemiBold" Click="btnNew_Click"/>

            <Button x:Name="btnDelete"
                        Content="Delete"
                        Width="100"
                        Height="36"
                        Margin="0,0,16,0"
                        Background="#EF4444"
                        Foreground="White" Click="btnDelete_Click"/>

            <TextBox x:Name="txtSearch"
                         Width="220"
                         Height="36"
                         Padding="8"
                         Margin="0,0,8,0"
                         VerticalContentAlignment="Center" TextChanged="txtSearch_TextChanged"/>

            <Button Content="Search"
                        Width="90"
                        Height="36"
                        Background="#F9D678"
                        FontWeight="SemiBold"/>

        </StackPanel>

        <!-- CONTENT -->

        <Grid Grid.Row="2">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>


            <!-- POSITION LIST -->

            <Border Grid.Column="0"
                    Background="White"
                    CornerRadius="12"
                    Padding="16"
                    Margin="0,0,16,0">

                <Border.Effect>
                    <DropShadowEffect BlurRadius="12" Opacity="0.15"/>
                </Border.Effect>

                <DataGrid x:Name="dgPositions"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          GridLinesVisibility="None"
                          RowHeight="34" SelectionChanged="dgPositions_SelectionChanged">

                    <DataGrid.Columns>

                        <DataGridTextColumn Header="ID"
                                            Binding="{Binding PositionId}"
                                            Width="1*"/>

                        <DataGridTextColumn Header="Position Name"
                                            Binding="{Binding PositionName}"
                                            Width="2*"/>

                        <DataGridTextColumn Header="Base Salary"
                                            Binding="{Binding BaseSalary}"
                                            Width="2*"/>

                    </DataGrid.Columns>

                </DataGrid>

            </Border>


            <!-- POSITION DETAIL -->

            <Border Grid.Column="1"
                    Background="White"
                    CornerRadius="12"
                    Padding="20">

                <Border.Effect>
                    <DropShadowEffect BlurRadius="12" Opacity="0.15"/>
                </Border.Effect>

                <StackPanel>

                    <TextBlock Text="Position Detail"
                               FontSize="20"
                               FontWeight="Bold"
                               Margin="0,0,0,20"/>


                    <!-- Position Name -->

                    <TextBlock Text="Position Name"/>
                    <TextBox x:Name="txtPositionName"
                             Height="34"
                             Padding="8"
                             Margin="0,6,0,14"/>


                    <!-- Base Salary -->

                    <TextBlock Text="Base Salary"/>
                    <TextBox x:Name="txtBaseSalary"
                             Height="34"
                             Padding="8"
                             Margin="0,6,0,20"/>


                    <!-- Buttons -->

                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Right">

                        <Button x:Name="btnAdd"
                                Content="Add"
                                Width="100"
                                Height="36"
                                Margin="0,0,8,0"
                                Background="#F9D678"
                                FontWeight="SemiBold" Click="btnAdd_Click"/>

                        <Button x:Name="btnUpdate"
                                Content="Update"
                                Width="100"
                                Height="36"
                                Background="#3B82F6"
                                Foreground="White"
                                FontWeight="SemiBold" Click="btnUpdate_Click"/>

                    </StackPanel>

                </StackPanel>

            </Border>

        </Grid>

    </Grid>

</Page>