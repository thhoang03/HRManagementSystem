<Page x:Name="frmDepartments" x:Class="HRManagementSystem.Views.Admin.DepartmentsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:HRManagementSystem.Views.Admin"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DepartmentsView" Loaded="frmDepartments_Loaded">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" 
                Margin="0,0,10,0" 
                Padding="10" 
                Background="White" 
                BorderBrush="#D0D7E2" 
                BorderThickness="1" 
                CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                    <Button Content="Refresh" Width="95" Margin="0,0,8,0"/>
                    <Button Content="New" Width="95" Margin="0,0,8,0"/>
                    <Button Content="Update" Width="95" Margin="0,0,8,0"/>
                    <Button Content="Delete" Width="95"/>
                </StackPanel>

                <DataGrid Grid.Row="1"
                          x:Name="dgDepartments"
                          AutoGenerateColumns="False"
                          IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding DepartmentId}" Width="90" />
                        <DataGridTextColumn Header="Name" Binding="{Binding DepartmentName}" Width="*" />
                        <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border Grid.Column="1"
                Padding="14" 
                Background="White" 
                BorderBrush="#D0D7E2" 
                BorderThickness="1" 
                CornerRadius="8">
            <StackPanel>
                <TextBlock Text="Department Detail" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,14" />

                <TextBlock Text="Department Name"/>
                <TextBox x:Name="txtDeptName"
                         Margin="0,6,0,2" Height="30"/>
                <TextBlock Text="Description"/>
                <TextBox x:Name="txtDescription"
                        Margin="0,6,0,2" Height="30"/>
                <Border Height="6" Background="Transparent" />

                <Button x:Name="btnSave" Content="Save" Height="34" Click="btnSave_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
