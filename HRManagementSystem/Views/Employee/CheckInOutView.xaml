<Page x:Class="HRManagementSystem.Views.Employee.CheckInOutView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Loaded="Page_Loaded"
      Background="#F8FAFC">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="My Attendance"
                   FontSize="24"
                   FontWeight="Bold"
                   Foreground="#0F172A"/>

        <Border Grid.Row="1"
                Background="White"
                CornerRadius="12"
                Padding="16"
                Margin="0,16,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock x:Name="txtToday"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="#0F172A"/>
                    <TextBlock x:Name="txtStatus"
                               Margin="0,8,0,0"
                               Foreground="#475569"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="btnCheckIn"
                            Content="Check In"
                            Width="110"
                            Height="36"
                            Margin="0,0,10,0"
                            Background="#22C55E"
                            Foreground="White"
                            BorderThickness="0"
                            FontWeight="SemiBold"
                            Click="btnCheckIn_Click"/>
                    <Button x:Name="btnCheckOut"
                            Content="Check Out"
                            Width="110"
                            Height="36"
                            Background="#F59E0B"
                            Foreground="White"
                            BorderThickness="0"
                            FontWeight="SemiBold"
                            Click="btnCheckOut_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2"
                Background="White"
                CornerRadius="12"
                Padding="16">
            <Border.Effect>
                <DropShadowEffect BlurRadius="12" Opacity="0.12"/>
            </Border.Effect>

            <DataGrid x:Name="dgAttendance"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      RowHeight="36"
                      GridLinesVisibility="None"
                      HeadersVisibility="Column"
                      BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date" Binding="{Binding AttendanceDate}" Width="*"/>
                    <DataGridTextColumn Header="Check In" Binding="{Binding CheckIn,StringFormat=g}" Width="*"/>
                    <DataGridTextColumn Header="Check Out" Binding="{Binding CheckOut,StringFormat=g}" Width="*"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*"/>
                    <DataGridTextColumn Header="Device IP" Binding="{Binding DeviceIp}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>

